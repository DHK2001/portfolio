export interface Skill {
  id: number;
  skillName: string;
  imageUrl: string;
}

export interface Profile {
  name: string;
  description: string;
  profilePicture: string;
}

export interface Project {
    projectName: string,
    description: string,
    image: string,
}